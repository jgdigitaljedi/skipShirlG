import React, { FunctionComponent } from 'react';
import './Login.scss';

const Login: FunctionComponent<any> = () => {
  return (
    <div className="login">
      <div>Login</div>
    </div>
  );
};

export default Login;
